class Solution {
public:
    int missingNumber(vector<int>& nums) {

    long long sumofn=((nums.size())*(nums.size()+1))/2;

    return sumofn-accumulate(nums.begin(),nums.end(),0);

        
    }
};